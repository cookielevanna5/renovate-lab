when:
  event:
    - push
  branch:
    - main

steps:
  renovate:
    image: renovate/renovate:latest
    environment:
      RENOVATE_PR_CONCURRENT_LIMIT: 0
      GITHUB_TOKEN:
        from_secret: cookie-github-token
    commands:
      - renovate
